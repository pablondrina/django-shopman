"""Payman Admin with Unfold theme.

Add 'shopman.payman.contrib.admin_unfold' to INSTALLED_APPS after 'shopman.payman'
to register the Unfold-styled versions. The core ``payman.admin`` skips registration
when this contrib is installed (avoids double registration).
"""
